## Time Complexity

Add Book:
O(n) because duplicate checking requires searching existing books.

View Books:
O(n) because every book is displayed.

Search Book:
O(n) because books are checked one by one.

Remove Book:
O(n) because book search is required.

Count Books:
O(n) because recursion visits each book once.